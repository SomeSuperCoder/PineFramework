## 1. Replace Pair Selection

- [x] 1.1 Replace pair `<select>` with text `<input>` in manual mode
- [x] 1.2 Auto-uppercase user input for pair symbol
- [x] 1.3 Add warning when symbol is not in `DEFAULT_SYMBOLS`

## 2. Replace Timeframe Selection

- [x] 2.1 Replace timeframe `<select>` with text `<input>` in manual mode
- [x] 2.2 Add quick-select chips for common timeframes (1m, 5m, 15m, 30m, 1h, 4h, 1d)
- [x] 2.3 Auto-fill input when chip is clicked

## 3. Validation

- [x] 3.1 Add validation: non-empty pair and timeframe required
- [x] 3.2 Add validation: timeframe must be a positive number
- [x] 3.3 Show error messages below inputs
