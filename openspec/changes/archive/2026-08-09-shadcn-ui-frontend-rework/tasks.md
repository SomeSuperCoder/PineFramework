## 1. Foundation — Tailwind v4 + shadcn scaffold + token bridge

- [x] 1.1 Bump Vite to ≥5.2 (5.4.x) in frontend/package.json; audit vite.config for PostCSS plugins; add devDeps: `tailwindcss`, `@tailwindcss/vite`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`; register `@tailwindcss/vite` plugin in `frontend/vite.config.ts` and `frontend/vitest.config.ts` — DONE design-system-engineer 2026-08-08: vite ^5.4.0 (resolved 5.4.21); added tailwindcss@^4.3.3, @tailwindcss/vite@^4.3.3, cva@^0.7.1, clsx@^2.1.1, tailwind-merge@^3.6.0, lucide-react@^1.30.0; plugin registered in both configs; no postcss config exists (none added)
- [x] 1.2 Add `@/` alias (`~/*` → `frontend/src`) in tsconfig.json + vite/vitest resolve.alias — DONE design-system-engineer 2026-08-08: `@/*` → `./src/*` in frontend/tsconfig.json paths + vite.config.ts + vitest.config.ts resolve.alias (frontend package only; root tsconfig untouched — it is the core package, no frontend alias)
- [x] 1.3 Run `shadcn init` (decline React 19 upgrade) — creates `frontend/src/components/ui/` + shadcn base CSS + `lib/utils.ts` (cn); add the shadcn component set — DONE design-system-engineer 2026-08-08: `shadcn init` run NON-interactively (--base radix, --preset nova, --css-variables, --no-monorepo); React 18 kept; components.json aliases use `@/`; 21 ui files created; shadcn base CSS landed in src/main.css; lib/utils.ts cn() verified; index.css :root and theme/tokens.ts byte-untouched
- [x] 1.4 Create `frontend/src/shadcn.css` bridge — DONE design-system-engineer 2026-08-08 (file named `main.css`, not shadcn.css — naming deviation recorded in board): shadcn theme variables → `var(--pf-*)` aliases (`--primary` = `var(--pf-ink-1)`, `--accent` = `var(--pf-brand-blue)`, etc.) + `@theme inline` mapping; imported in `main.tsx` AFTER index.css; `index.css` `:root` untouched
- [x] 1.5 Design-law override layer — DONE design-system-engineer 2026-08-08 (+fix-wave design-system-engineer 2026-08-08): pill radius `[data-slot="button"]`, input radius md + 44px, `--pf-focus-ring` on `:focus-visible`, disabled `cursor:not-allowed` + disabled label lifted to `--pf-steel-muted` (contrast fix), weights ≤600 (fix wave: `font-bold`/700 → semibold/600 swept across converted files), dark-only `@custom-variant dark (&:where(:root, :root *))`, motion 150/200ms, tabular-nums, reduced-motion kill-switch
- [x] 1.6 Extend `token-mirror.test.ts` (or sibling) — DONE test-engineer 2026-08-08: `frontend/src/__tests__/shadcn-bridge.test.ts` asserts shadcn vars reference `var(--pf-*)` (hex-free bridge); `:root` mirror green; 260/260 suite GREEN exit 0. NOTE: weight-cap conformance scan currently covers `ui/` only — page-component scan extension saved to recommendations (2026-08-08-weight-cap-scan-scope)
- [x] 1.7 W1 exit gate — DONE tech-lead 2026-08-08 (build + test parts): `pnpm build` exit 0 (tsc && vite build), `pnpm test` 260/260 exit 0 (TE3). Playwright smoke + `ui/README.md` manifest deferred to e2e wave (task 2.9/5.1) — not run this session

## 2. Primitives — shadcn component adoption + swap

- [x] 2.1 Button + IconButton — DONE frontend-engineer lanes 2026-08-08: variants per DESIGN §15.1 (default=white pill via --primary, secondary, outline, ghost, accent-yellow ≤1/viewport, destructive); sizes law-enforced (default h-11/44px, sm h-10/40px, lg h-12, icon size-11; dense 24/28px kept for DESIGN-documented exceptions: breadcrumbs, legend icons, dialog close, steppers); rounded-full; loading aria-busy; toggle aria-pressed; callers swapped across chrome/panels/overlays/bot
- [x] 2.2 Input + Textarea + Label + NumberInput — DONE frontend-engineer lanes 2026-08-08: 44px inputs/textarea via `[data-slot]` law; NumberInput renders shadcn Input + steppers with ArrowUp/Down keyboard adjust (BacktestGeneralSettings NumberField); aria-invalid/aria-describedby on error fields (BacktestPanel); placeholder token
- [x] 2.3 Select (Radix) + Autocomplete Command — DONE frontend-engineer lanes 2026-08-08: shadcn Select for AppToolbar/TradeTabShared StatusSelect/SettingsPanel/StatisticsTab; StrategySelector bespoke combobox → shadcn Command in Popover (kbd nav, viewport scroll, data-selected highlight)
- [x] 2.4 Dialog/AlertDialog/Popover/Tooltip — DONE frontend-engineer lanes 2026-08-08: QuickAdderPopup (Command+Popover), GoToDatePopup (Popover), StrategyConflictDialog (AlertDialog), StrategyResultsPopup (Dialog), CodeEditor overlay shell (Dialog large; editor core + Ctrl+Enter autosave + dirty guard kept); Tooltip on chrome icon buttons; focus trap/restore/aria-modal from Radix; ErrorConsole bespoke modal hardened (Escape + trap + focus restore) per audit
- [x] 2.5 Tabs + Switch — DONE frontend-engineer lanes 2026-08-08: shadcn Tabs (roving tabindex, arrows, aria-selected) for LiveDashboard, TradeTabShared ModeToggle (All|Live|Chaos), BacktestGeneralSettings; Switch role="switch" for bot/settings toggles
- [x] 2.6 Badge + Skeleton + Alert + Card — DONE frontend-engineer lanes 2026-08-08: Badge (StrategySelector types, AutoSelectGrid StatusIcon, ChartComponent legend, BacktestCommissionSettings); Alert (ChaosModeWarning destructive, ErrorConsole, TelegramConfigPanel status); Card recipes (BotMetrics tiles, BacktestResults stats, StatisticsTab, MiniChart shell); Skeleton not introduced where loading text + aria-busy preserved (noted — optional)
- [x] 2.7 Progress + Table + Separator/Toolbar — DONE frontend-engineer lanes 2026-08-08: Progress (ProgressBar §15.7 status variants + motion-reduce indeterminate); shadcn Table (TradeHistoryTab, StatisticsTab, BacktestResults trade list, AutoSelectGrid); DropdownMenu (BacktestResults Export)
- [x] 2.8 E1 unit-test migration sweep — DONE test-engineer 2026-08-08: 27 outdated-test/env-gap fixes (button→tab roles, native select→Radix combobox, popover-content, logger WebSocket stub + exit-code fix); full suite 260/260 GREEN exit 0
- [ ] 2.9 e2e update + re-baseline — NOT DONE (Playwright e2e wave — trade-dashboard.spec.ts selectors, chunk-boundary, visual goldens) — pending

## 3. Chrome — application shell

- [x] 3.1 TopBar — DONE frontend-engineer lane 2 2026-08-08: 48px shell, wordmark → `--pf-brand-yellow` (§16 fix), ghost icon buttons 44px + Tooltip, StatusDot `role="status"` + aria-label + 10px (§15.8), error badge as text; `data-testid="topbar"` kept
- [x] 3.2 Sidebar — DONE frontend-engineer lane 2 2026-08-08 (visual + a11y): rail 64↔220, aria-expanded, aria-current="page", active = surface-2 + ink-1 + brand-blue bar (not red), lucide icons, labels hidden rail with aria-label. ⚠️ click-expand persistent toggle + ArrowRight/Left/Escape keyboard parity NOT delivered (pre-existing gap F4; recommendation 2026-08-08-shadcn-frame-keyboard-nav)
- [x] 3.3 ControlPanel — DONE frontend-engineer lanes 2026-08-08: role="region" + aria-label per panel; global 1–5 routing kept byte-identical; `role="application"` anti-pattern removed; segmented pills via rail; trailing newline
- [x] 3.4 ContentArea + AppToolbar — DONE frontend-engineer lane 2 2026-08-08: breadcrumb bar via link Buttons + hairline; toolbar shadcn Select + ghost buttons + tokens (no legacy hex); Backtest CTA blue variant white text (§15.1 contrast fix); data-testid contracts kept
- [ ] 3.5 Chrome exit gate — NOT DONE (keyboard walk, landmarks walk-through, screenshot parity vs baseline) — partially covered by UX audit; formal gate pending e2e wave

## 4. Panels & views

- [x] 4.1 Backtest cluster — DONE frontend-engineer lane 3/4b 2026-08-08: BacktestPanel, BacktestGeneralSettings, BacktestCommissionSettings, StrategyConflictDialog — shadcn forms (Input/Select/NumberInput/Button), localStorage + param state kept, validation role=alert, Progress via ProgressBar; NumberField ArrowUp/Down + label binding (fix wave)
- [x] 4.2 Strategy cluster — DONE frontend-engineer lane 3/F2a 2026-08-08: StrategySelector → Command/Popover + raw-paste area (ghost button, tokens); StrategyResults → Dialog/Card; states loading/error/empty/selected; legacy classes + hex removed
- [x] 4.3 Live cluster — DONE frontend-engineer lane 4a 2026-08-08: LiveDashboard (Tabs), BotStatusPanel, BotControls, BotMetrics, MetricValue, AutoSelectGrid, ChaosModeWarning, TradingBotPanel facade — Badges, Cards, Alert, tokens, test-critical text preserved; ChaosModeWarning contrast fixed (white pill ink text)
- [x] 4.4 Data cluster — DONE frontend-engineer lanes 3/4b/F2b 2026-08-08: TradeHistoryTab (row virtualization kept, shadcn Table, sortable header button + aria-sort), StatisticsTab (cards + select + tabs; canvas charts untouched), AutoSelectGrid — e2e trade-dashboard pairs gate pending (2.9)
- [x] 4.5 Infra cluster — DONE frontend-engineer lanes 3/F2b 2026-08-08: TelegramConfigPanel (fields, Save&Test states + role=status feedback + aria-busy + ≥44px touch), SettingsPanel (Switch/Select/Input), ErrorConsole (three-tier alert + aria-live + focus trap/Escape, "All clear" empty state), GoToDatePopup
- [x] 4.6 Chart chrome (NOT engine) — DONE frontend-engineer lane 5 2026-08-08: ChartComponent legend → Badge + ghost icon Buttons + Tooltip + Loader2 spinner; MiniChart outer shell → Card; engine `frontend/src/chart/**` zero edits (git diff empty); `window.__pineChart` bridge + canvas count verified
- [x] 4.7 Panels exit gate — DONE tech-lead 2026-08-08 (unit parts): full `pnpm test` 260/260 GREEN exit 0, `pnpm build` exit 0, no raw legacy hex in converted files (fix wave sweep). e2e (trade-dashboard + chunk-boundary) pending (2.9)

## 5. Verification, a11y, code review, QA

- [x] 5.1 Test Engineer (unit parts) — DONE test-engineer 2026-08-08: full unit suite 260/260 GREEN exit 0; tests migrated to role/data-testid; logger exit-code fix. e2e regen + launch-test Playwright flows pending (2.9)
- [x] 5.2 UX + UI Designer a11y audit — DONE ux-designer 2026-08-08: contrast matrix (3 blockers fixed pre-commit), focus visible, keyboard flows, states (UX 5), reduced-motion, 44px touch; 25 findings triaged (blockers fixed, deferred → recommendations/accessibility/2026-08-08-shadcn-a11y-deferred.md)
- [x] 5.3 Code Reviewer — DONE code-reviewer 2026-08-08: diff vs specs — weight cap swept, legacy hex swept, no light theme, dead `.badge`/`.quick-adder-*` CSS removed, cn/recipe layering sound, no duplicated literals; 4 blockers + 5 majors fixed in fix wave
- [x] 5.4 QA Engineer — DONE qa-engineer 2026-08-08: ⚠️ GO-WITH-CONDITIONS → conditions (legacy classes, hex, weight) satisfied in fix wave → GO; acceptance criteria verified (shadcn-to-max, DESIGN.md maintained, behavior preserved, accessible names)
- [x] 5.5 Tech Lead commit — DONE tech-lead 2026-08-08: `feat(frontend): adopt shadcn/ui layer across the app`; tasks.md updated with evidence; recommendations persisted to `recommendations/` (12 files)

## 6. Rollback & follow-ups

- [ ] 6.1 Rollback verified — NOT DONE (revert path: remove tailwind plugin + main.css + deps; component swaps revertible per-file — documented in proposal, not exercised)
- [x] 6.2 Recommendations saved — DONE tech-lead 2026-08-08: 12+ pending recommendations across accessibility/architecture/quality/ux (keyboard rail nav, panel switcher decision, a11y deferred sweep, app-tsx hex, pagination touch, telegram badge colors, weight-cap scan scope, number-input dedupe, motion token, command-input ref, vitest ws, command-input forwardRef)
- [x] 6.3 Save architecture decision to AgentMemory — DONE tech-lead 2026-08-08: "PineFramework shadcn/ui migration ADR — Tailwind v4 + shadcn layer, pf-tokens SSOT, white-primary pill per DESIGN §15.1" (type=architecture)
