## No Spec Changes

This change removes implementation artifacts only — no spec-level requirements are
added, modified, or removed. The affected specs (`strategy-backtest-engine`,
`cli-backtest-tool`) reference "commission" generically without enumerating specific
method IDs, so their requirements are unchanged.
